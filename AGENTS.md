After making any changes to `tab-start.plugin.zsh`, run `just check`.

After making any changes to the available options, update `scripts/benchmark.zsh`.

After making changes to `scripts/benchmark.zsh` or anything possibly performance-relevant in `tab-start.plugin.zsh`, run `just update-readme-benchmark`.
